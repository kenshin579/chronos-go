# v1.0.0 준비 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 공개 API를 감사·보수적 수정하고 v1 문서를 갖춰 v1.0.0을 선언한다(기능 추가 없음).

**Architecture:** 감사(Phase A, 읽기 전용 findings 리포트) → 체크포인트(사용자 결정) → 보수적 수정(Phase B) → v1 문서(Phase C) → 3중 게이트+태그(Phase D). 감사가 수정 내용을 결정하므로 Phase B는 감사 산출에 의존한다(사전 확정 후보는 pause 큐 이름 처리 1건, 아래 분석 포함).

**Tech Stack:** Go 1.26, 기존 chronos 코어. 문서는 godoc Example(example_test.go)·doc.go·Markdown.

**스펙:** `docs/superpowers/specs/2026-07-15-v1-prep-design.md`

**전제:**
- 브랜치 `chore/v1-prep`. 로컬 Redis 필요, 테스트 `-p 1`.
- **메인 작업 트리에서 git checkout/switch 금지.**
- 커밋 author `-c user.email=kenshin579@hotmail.com -c user.name=kenshin579`, 메시지 끝 빈 줄 후 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, HEREDOC.
- 단일 PR(감사+수정+문서). 태그는 머지 후 별도(4h 소크 게이트).

**공개 API 심볼(감사 대상, 확인됨):** Client(Close)·Chain(Then/ThenGroup/Enqueue)·Group(Add/AddChain/OnComplete/Enqueue)·Inspector(Queues/ListTasks/GetTask/RunTask/DeleteTask/PauseQueue/ResumeQueue/PausedQueues/GroupMembers/SchedulerStatus)·Server(NewServer/Start/Shutdown/ServerConfig)·Scheduler(NewScheduler/Start/Shutdown/SchedulerConfig/RegisterInterval/RegisterCron/MisfirePolicy)·Task[T](ID/Queue/Args/RawGroupResults)·TaskArgs·Mux(NewMux)·AddHandler/AddHandlerR·Enqueue·PrevResult/GroupResults·옵션 10종(WithQueue/WithTaskID/WithMaxRetry/WithDeadLetterDiscard/WithProcessIn/WithProcessAt/WithUnique/WithRetention/WithMisfirePolicy)·에러(SkipRetry/ErrNoResult/ErrResultTooLarge/ErrDuplicateTask/ErrTaskNotFound/ErrInvalidState)·MaxResultSize·DefaultRetryDelay/RetryDelayFunc·Metrics·TaskOutcome·반환 타입(QueueInfo/TaskInfo/ScheduleInfo/SchedulerStatus/GroupInfo)

---

### Task 1: API 감사 (읽기 전용 → findings 리포트)

**Files:**
- Create: `docs/superpowers/v1-api-audit.md` (findings 리포트 — 코드 변경 없음)

이 태스크는 **코드를 바꾸지 않는다.** 공개 API를 6그룹으로 나눠 감사하고 발견을 분류한 리포트만 만든다. (컨트롤러는 이 태스크를 6개 병렬 감사 서브에이전트로 실행하고 종합해도 되고, 단일 감사로 해도 된다. 산출물 형식만 고정.)

- [ ] **Step 1: 6그룹 감사 수행** — 각 그룹을 아래 점검표로 훑는다:
  - ① 옵션 10종  ② 빌더 Chain/Group  ③ Inspector + 반환 타입  ④ Server/Scheduler config  ⑤ 제네릭 Task[T]/Mux/AddHandler(R)/Enqueue/Register/PrevResult/GroupResults  ⑥ 에러·기타

  점검 항목(각 심볼):
  - **네이밍 일관성**: 같은 개념이 곳곳에서 같은 이름인가 (예: `ID` vs `TaskID`, `Queue` vs `QueueName`, `Kind` 표기)
  - **시그니처 에르고노믹스**: 제네릭 타입파라미터 순서(예 `PrevResult[R any, T TaskArgs]` — R만 명시하고 T가 추론되는가), ctx 첫 인자 일관, 반환 형태
  - **에러 계약**: 노출된 센티넬이 `errors.Is`로 래핑·검사 가능한가, godoc에 문서화됐는가, 반환 위치 일관
  - **godoc ↔ 코드 일치**: 주석이 실제 동작을 정확히 기술하는가(특히 기본값·부작용·재전달 caveat)
  - **zero-value/기본값**: config 제로값이 안전한 기본으로 귀결되는가(`ServerConfig{}`, `SchedulerConfig{}`)
  - **하위호환**: v0.x에서 쓰던 시그니처를 깨는 변경이 필요한 항목인가

- [ ] **Step 2: findings 리포트 작성** — `docs/superpowers/v1-api-audit.md`에 다음 형식:

  ```markdown
  # v1.0.0 공개 API 감사

  ## (i) 결함/불일치 — 수정 대상
  - [심볼] 문제 / 근거 / 제안 수정 (breaking 여부)

  ## (ii) 애매 — 사용자 결정 필요
  - [심볼] 관찰 / 선택지 A vs B / 트레이드오프

  ## (iii) 확인됨 — 그대로 동결
  - 그룹별 한 줄 요약 (문제 없음 확인)
  ```

  **pause 큐 이름 항목은 반드시 (ii)에 포함** — 아래 분석을 근거로:
  > `Inspector.PauseQueue`가 임의 큐 이름을 받아 `chronos:paused` SET에 고스트 멤버를 남길 수 있다. 단, `chronos:queues`(등록 큐 SET)는 **enqueue 시점 lazy 등록**이라, 태스크가 아직 없는 "설정만 된 큐"를 pause하는 정당한 경우를 등록-여부 검증이 **거짓 거부**한다. 따라서 코어 `PauseQueue`에 검증을 넣는 것은 위험. 선택지 — **A) 문서화만**(고스트 멤버는 `resume`으로 복구 가능, UI엔 미표시라 무해), **B) CLI/webui 계층에서만** 알려진 큐가 아니면 경고(코어는 그대로). 권고: A 또는 B, 코어 검증은 하지 않음.

- [ ] **Step 3: 커밋**

```bash
git add docs/superpowers/v1-api-audit.md
git commit -m "docs: v1 공개 API 감사 findings 리포트"
```

**→ 컨트롤러 체크포인트**: (i)/(ii)를 사용자에게 보고하고 각 항목 처리를 확정한 뒤 Task 2로. (i)/(ii)가 비면 Task 2는 pause 항목의 확정 처리(대개 "문서화")만 수행.

---

### Task 2: 감사 결함 수정 (보수적)

**Files:** 감사 findings의 (i) + 체크포인트에서 확정된 (ii)에 따라 결정.

이 태스크의 구체 내용은 **Task 1 산출 + 체크포인트 결정**으로 확정된다. 각 확정 수정은 아래 **패턴**을 따른다(TDD):

- [ ] **각 확정 수정마다**: (1) 동작을 고정하는 실패 테스트 작성 → (2) 실패 확인 → (3) 최소 수정 → (4) `go test -p 1 -count=1 -run <패턴> .` + `go vet ./...` 통과 → (5) 커밋 `fix: <심볼> <내용>` (breaking이면 메시지에 `BREAKING:` 접두).

- [ ] **pause 큐 이름 (확정 후보)**: 체크포인트 결정에 따라 —
  - **문서화(A)로 결정 시**: `Inspector.PauseQueue` godoc에 "존재하지 않는 큐 이름도 받으며, 그 경우 `ResumeQueue`로 해제할 때까지 paused 목록에 남는다(활성 큐에는 무영향)" 한 줄 추가. `cmd/chronos`의 `queue pause` 도움말에도 반영. 테스트 불필요(문서), README Known limitations에 반영은 Task 5에서.
  - **CLI 경고(B)로 결정 시**: `cmd/chronos/main.go`의 `queue pause` 핸들러에서 `insp.Queues()`에 없는 이름이면 stderr 경고 후 계속(거부 아님 — 설정만 된 큐 정당). 코어 `PauseQueue`는 불변.

- [ ] **감사 (i) 항목들**: 각 항목을 위 패턴으로. **보수적 기준상 0~소수 예상.** 없으면 이 태스크는 pause 처리만으로 종료.

- [ ] **완료 확인**: `make check` 그린.

(주의: 이 태스크는 감사 결과 의존이라 사전에 완전한 코드를 담을 수 없다. 컨트롤러는 체크포인트 후 확정된 각 수정을 위 패턴의 개별 커밋으로 구현한다. breaking 수정은 v1 전 마지막 기회이므로 findings에서 명시적으로 승인된 것만.)

---

### Task 3: doc.go — 패키지 개요

**Files:**
- Create: `doc.go`

- [ ] **Step 1: doc.go 작성** — pkg.go.dev 첫 화면이 될 패키지 주석. 핵심 개념 지도:

```go
// Package chronos is a Redis-backed distributed task queue and scheduler.
//
// A [Client] enqueues tasks; a [Server] consumes them by routing each to a
// handler registered on a [Mux]. Tasks are strongly typed: define an args type
// implementing [TaskArgs], register a handler with [AddHandler] (or
// [AddHandlerR] to return a result), and enqueue with [Enqueue].
//
//	type EmailArgs struct{ To string }
//	func (EmailArgs) Kind() string { return "email:send" }
//
//	mux := chronos.NewMux()
//	chronos.AddHandler(mux, func(ctx context.Context, t *chronos.Task[EmailArgs]) error {
//		return send(t.Args.To)
//	})
//	srv := chronos.NewServer(rdb, chronos.ServerConfig{Concurrency: 10})
//	srv.Start(ctx, mux)
//	chronos.Enqueue(ctx, client, EmailArgs{To: "a@b.c"})
//
// # Workflows
//
// [NewChain] runs tasks in sequence (each link starts when its predecessor
// succeeds); [NewGroup] fans out tasks in parallel and fires an OnComplete
// callback when all finish. Results flow between steps: a handler registered
// with [AddHandlerR] exposes its result to the next chain link via
// [PrevResult] and to a group callback via [GroupResults]. [Chain.ThenGroup]
// embeds a parallel stage inside a chain, and [Group.AddChain] makes a group
// member a chain — together they express fan-out/fan-in pipelines.
//
// # Scheduling
//
// [NewScheduler] runs periodic jobs (interval or cron) with leader election, so
// many instances may run a scheduler and only one fires each trigger.
//
// # Inspection
//
// [NewInspector] reads queue stats, lists and re-runs tasks, pauses queues, and
// reports scheduler state — the read/operate surface behind the CLI and web
// console.
//
// # Delivery semantics
//
// Delivery is at-least-once: a handler may run more than once (redelivery after
// a crash, or a recoverer reclaiming a stalled task), so handlers must be
// idempotent. See the module README for the full list of guarantees and
// caveats.
//
// # Stability
//
// From v1.0.0 the core package follows semantic versioning. The contrib
// modules (contrib/webui, contrib/prometheus) and internal/ are experimental
// and not covered by that guarantee.
package chronos
```

- [ ] **Step 2: 빌드·vet 확인**

Run: `go build ./... && go vet ./... && gofmt -l doc.go`
Expected: 무출력. (`go doc .`로 렌더 육안 확인 권장.)

- [ ] **Step 3: 커밋**

```bash
git add doc.go
git commit -m "docs: doc.go 패키지 개요 (pkg.go.dev 첫 화면)"
```

---

### Task 4: example_test.go — godoc 실행 예제

**Files:**
- Create: `example_test.go`

- [ ] **Step 1: 예제 파일 작성** — `package chronos_test`(외부 테스트 패키지 — 사용자 관점 import 경로 검증). 6개 예제. Redis에 실제 연결하는 예제는 `// Output:` 주석을 **생략**한다(godoc 렌더용, `go test`는 Output 없는 Example를 컴파일만 하고 실행 안 함 → CI에서 Redis 불요). 순수 계산 예제만 `// Output:`을 단다.

핵심: 아래 형태로 6개. 첫 예제 전문 + 나머지는 동일 골격:

```go
package chronos_test

import (
	"context"
	"fmt"
	"time"

	"github.com/redis/go-redis/v9"

	"github.com/kenshin579/chronos-go"
)

type EmailArgs struct {
	To string `json:"to"`
}

func (EmailArgs) Kind() string { return "email:send" }

// Example shows the enqueue → handler round trip: define a typed args, register
// a handler on a Mux, start a Server, and enqueue.
func Example() {
	rdb := redis.NewClient(&redis.Options{Addr: "127.0.0.1:6379"})
	ctx := context.Background()

	mux := chronos.NewMux()
	chronos.AddHandler(mux, func(ctx context.Context, t *chronos.Task[EmailArgs]) error {
		fmt.Println("sending to", t.Args.To)
		return nil
	})

	srv := chronos.NewServer(rdb, chronos.ServerConfig{Concurrency: 10})
	_ = srv.Start(ctx, mux)
	defer srv.Shutdown(context.Background())

	client := chronos.NewClient(rdb)
	_, _ = chronos.Enqueue(ctx, client, EmailArgs{To: "a@b.c"}, chronos.WithQueue("default"))
	// Output는 생략: 실제 Redis·비동기 처리라 결정적 출력이 아님.
}

type ResizeArgs struct {
	Path string `json:"path"`
}

func (ResizeArgs) Kind() string { return "img:resize" }

type ResizeResult struct {
	Bytes int `json:"bytes"`
}

// ExampleAddHandlerR shows a handler that returns a result, which the next
// workflow step reads with PrevResult / GroupResults.
func ExampleAddHandlerR() {
	mux := chronos.NewMux()
	chronos.AddHandlerR(mux, func(ctx context.Context, t *chronos.Task[ResizeArgs]) (ResizeResult, error) {
		return ResizeResult{Bytes: 1024}, nil
	})
	_ = mux
}

// ExampleNewChain runs tasks in sequence; each link starts after the previous
// one succeeds.
func ExampleNewChain() {
	rdb := redis.NewClient(&redis.Options{Addr: "127.0.0.1:6379"})
	client := chronos.NewClient(rdb)
	_, _ = chronos.NewChain().
		Then(ResizeArgs{Path: "a.png"}).
		Then(ResizeArgs{Path: "b.png"}).
		Enqueue(context.Background(), client)
}

// ExampleNewGroup fans out members in parallel, then fires OnComplete once all
// succeed. ThenGroup embeds this as a chain stage; AddChain makes a member a
// chain.
func ExampleNewGroup() {
	rdb := redis.NewClient(&redis.Options{Addr: "127.0.0.1:6379"})
	client := chronos.NewClient(rdb)
	_, _ = chronos.NewGroup().
		Add(ResizeArgs{Path: "a.png"}).
		AddChain(chronos.NewChain().Then(ResizeArgs{Path: "b.png"}).Then(ResizeArgs{Path: "b2.png"})).
		OnComplete(ResizeArgs{Path: "manifest"}).
		Enqueue(context.Background(), client)
}

// ExampleNewScheduler registers a periodic job. Every instance may call Start;
// leader election ensures only one fires each trigger.
func ExampleNewScheduler() {
	rdb := redis.NewClient(&redis.Options{Addr: "127.0.0.1:6379"})
	s := chronos.NewScheduler(rdb, chronos.SchedulerConfig{})
	_ = chronos.RegisterInterval(s, time.Hour, EmailArgs{To: "digest@b.c"})
	_ = s.Start(context.Background())
	defer s.Shutdown(context.Background())
}

// ExampleNewInspector reads operational state: queue stats, task listing,
// pause/resume, scheduler status.
func ExampleNewInspector() {
	rdb := redis.NewClient(&redis.Options{Addr: "127.0.0.1:6379"})
	insp := chronos.NewInspector(rdb)
	queues, _ := insp.Queues(context.Background())
	for _, q := range queues {
		fmt.Printf("%s: pending=%d paused=%v\n", q.Queue, q.Pending, q.Paused)
	}
}
```

(예제는 컴파일 검증이 핵심이므로 시그니처가 실제와 맞는지가 중요. `AddChain`/`ThenGroup`/`AddHandlerR`/`PrevResult`/`GroupResults` 실제 시그니처에 맞춰 구현 시 조정. Output 없는 Example은 `go test`가 컴파일만 함.)

- [ ] **Step 2: 컴파일·vet 확인**

Run: `go test -p 1 -count=1 -run 'Example' . && go vet ./... && gofmt -l example_test.go`
Expected: PASS(Output 없는 예제는 실행 스킵, 컴파일만), 무출력. (Redis 없어도 통과 — Output 없는 Example은 실행 안 됨.)

- [ ] **Step 3: 커밋**

```bash
git add example_test.go
git commit -m "docs: godoc 실행 예제 6종 (기본/결과반환/체인/그룹/스케줄러/인스펙터)"
```

---

### Task 5: README Stability + 알려진 한계 통합 + contrib 문구

**Files:**
- Modify: `README.md`
- Modify: `contrib/webui/README.md`, `contrib/prometheus/README.md`

- [ ] **Step 1: README에 Stability & Compatibility 소절 추가** — `## Requirements` 근처(또는 `## Known limitations / roadmap` 앞)에:

```markdown
## Stability & compatibility

From **v1.0.0**, the core package (`github.com/kenshin579/chronos-go`) follows
[semantic versioning](https://semver.org): breaking changes to its public API
land only in a new major version. APIs slated for removal are marked with a
`// Deprecated:` godoc comment for at least one minor release first.

Not covered by this guarantee: the `contrib/` modules (`contrib/webui`,
`contrib/prometheus`) — experimental, versioned separately — and any `internal/`
package. The core `Metrics` hook (zero dependencies) is part of the core API.

Supported Go versions: the two most recent stable releases.
```

- [ ] **Step 2: 알려진 한계 통합** — 기존 `## Known limitations / roadmap` 섹션을 흩어진 caveat를 모은 형태로 정리(중복 제거, 누락 보강):

```markdown
## Known limitations / roadmap

Delivery is **at-least-once** — a handler may run more than once (crash
redelivery, or a recoverer reclaiming a task idle longer than `RecoverMinIdle`),
so handlers must be idempotent.

- **Scheduler fencing** relies on a dedup TTL (`10 × LeaderTTL`), not a fencing
  token: a leader paused longer than that window and then resumed could double-
  fire a trigger. Instances must share one clock and `Location`.
- **Unique locks** cover in-flight tasks via heartbeat; a task waiting in the
  retry/scheduled set is covered only by its lock TTL, so set the TTL to exceed
  the task's total lifetime.
- **Queue pause** takes effect within ~1s (server-side cache). `PauseQueue`
  accepts any queue name; an unknown name lingers in the paused set until
  `ResumeQueue` clears it (no effect on active queues).
- **Workflow nesting is one level**: a group member may be a chain (`AddChain`),
  and a chain may contain a parallel stage (`ThenGroup`), but not deeper (a
  member chain cannot contain a `ThenGroup`, and a group cannot nest a group).
- **Results** are capped at 1 MiB (`MaxResultSize`); pass a reference for larger
  artifacts. Results are relay-only (no out-of-workflow result store).

Roadmap (not yet built): asynq→chronos migration guide, official Sentinel
support, deeper workflow nesting.
```

(기존 섹션 내용과 대조해 이미 있는 문장은 재사용·정리하고, 위 목록이 최종본이 되게 한다. pause 항목 문구는 Task 2 체크포인트 결정과 일치시킬 것.)

- [ ] **Step 3: contrib README 실험적 문구** — `contrib/webui/README.md`와 `contrib/prometheus/README.md` 최상단에:

```markdown
> **Experimental.** This contrib module is versioned separately and is **not**
> covered by the core library's v1 stability guarantee. Its API and behavior
> may change between releases.
```

- [ ] **Step 4: 확인 + 커밋**

Run: `file -I README.md contrib/webui/README.md contrib/prometheus/README.md | grep -v utf-8 || echo UTF-8 ok`
Expected: `UTF-8 ok`

```bash
git add README.md contrib/webui/README.md contrib/prometheus/README.md
git commit -m "docs: README Stability & compatibility + 알려진 한계 통합 + contrib 실험적 문구"
```

---

### Task 6: CONTRIBUTING.md

**Files:**
- Create: `CONTRIBUTING.md`

- [ ] **Step 1: CONTRIBUTING.md 작성**

```markdown
# Contributing

Thanks for helping improve chronos-go.

## Prerequisites

- Go (the two most recent stable releases)
- A local Redis for tests: `redis-server --port 6379` (or Docker)

## Running tests

```bash
make check          # fmt-check + vet + race tests + contrib + benchmarks build
go test ./... -p 1  # -p 1 is required: packages share Redis DB 15 and flush it,
                    # so parallel package execution clobbers state
```

## Redis Cluster (optional)

Cluster-safety smoke tests run against a disposable local cluster:

```bash
cd deploy/redis-cluster && docker compose up -d   # 6-node cluster
make test-cluster                                  # opt-in; skipped without the env
```

## Soak test (optional)

Long-running leak check (heap / goroutine / Redis keyspace trend):

```bash
make soak                                    # 1 hour
cd benchmarks && go run ./cmd/soak -duration 4h   # before a major release
```

## Pull requests

- Branch from `main`; never commit to `main` directly.
- Keep the public API stable (see the Stability section in the README). Breaking
  changes to the core package require a major version and prior discussion.
- Include tests; `make check` must be green.
- New features generally follow spec → plan → implementation (see
  `docs/superpowers/`).
```

- [ ] **Step 2: 확인 + 커밋**

Run: `file -I CONTRIBUTING.md | grep -q utf-8 && echo ok`

```bash
git add CONTRIBUTING.md
git commit -m "docs: CONTRIBUTING.md (셋업·테스트·cluster·소크·PR 규칙)"
```

---

### Task 7: 최종 검증 + PR

**Files:** 없음(검증·PR)

- [ ] **Step 1: 전체 검증**

Run: `make check`
Expected: 전체 그린. (예제 컴파일·doc.go·기존 테스트 모두 포함.)

- [ ] **Step 2: cluster 스모크** (docker 필요 — 없으면 `deploy/redis-cluster` 기동 후)

Run: `make test-cluster`
Expected: 20/20 PASS

- [ ] **Step 3: PR 생성** — base main, assignee kenshin579, HEREDOC. 제목 `chore: v1.0.0 준비 — API 감사·문서·안정성 보증`. 본문에 감사 요약(수정 N건), 문서 산출물, "태그는 머지 후 4h 소크 게이트 통과 시 v1.0.0" 명시.

---

## 완료 후 (Phase D — 태그 게이트, 머지 후 main에서)

1. main 최신화(`git checkout main && git pull`).
2. `make check` 그린.
3. cluster 스모크 20/20 (`deploy/redis-cluster` + `make test-cluster`).
4. **4시간 소크**: `cd benchmarks && go run ./cmd/soak -duration 4h` — heap/goroutine/DBSIZE 3지표 PASS. 판정 표 보관(릴리스 노트 첨부용). (백그라운드 실행 + Monitor로 판정 대기.)
5. `v1.0.0` 태그 + GitHub 릴리스:
   - 기능 요약(asynq 패리티 + 워크플로 체인/그룹/결과전달/ThenGroup/그룹멤버체인 + 성능 2.5~4x + 관찰 3축 + cluster)
   - Stability 보증 문구
   - 4h 소크 판정 표
   - `go get github.com/kenshin579/chronos-go@v1.0.0`
6. 사용자에게 태그 발행 승인 요청 후 태그.

## 최종 리뷰

Task 7 전(또는 PR 생성 후 CI 대기 중) k:code-reviewer로 브랜치 최종 리뷰: 문서 정확성(doc.go/README/예제가 실제 API와 일치, deprecated 문구 없음), 감사 수정의 회귀 없음, contrib 문구, 알려진 한계 통합의 정확성. 지적 반영 후 머지 승인 요청.
